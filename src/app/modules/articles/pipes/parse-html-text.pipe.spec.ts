import { ParseHtmlTextPipe } from './parse-html-text.pipe';

describe('ParseHtmlTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseHtmlTextPipe();
    expect(pipe).toBeTruthy();
  });
});
