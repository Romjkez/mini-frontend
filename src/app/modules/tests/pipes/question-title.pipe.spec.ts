import { QuestionTitlePipe } from './question-title.pipe';

describe('QuestionTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
